// Admin filter types (user list + sessions list) will be authored here in Phase 1 + Phase 3.
// Mirrors the orders slice's filters.ts shape (StagedFilters / DEFAULT_FILTERS / ListParams /
// toListParams / filtersEqual). Self-flow needs no filters and contributes nothing here.

export {};
